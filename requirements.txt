# Minimal dependency set required to run experiments 1-6.
# Tested with Python 3.11 / 3.12.

torch>=2.2.0
transformers>=4.40.0
accelerate>=0.27.0
datasets
scipy
numpy
matplotlib
seaborn
tqdm
pyyaml
huggingface_hub

# Optional — only needed for specific components:
#   pytest           (for tests/)
#   scikit-learn     (for t-SNE plot in src/visualization/token_embedding_tsne.py)
#   openai           (for APIJudge in src/evaluation/judge.py)
#   anthropic        (for APIJudge in src/evaluation/judge.py)
#   wandb            (for experiment tracking)
#   bitsandbytes     (for INT8/INT4 quantization on CUDA)
